# Default values for ..
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: vault
  tag: 1.2.3
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8200

configSecret: "vault-config"

resources: {}

nodeSelector: {}

tolerations: []

affinity: {}
